// let score;
// console.log(typeof score);
// console.log(typeof (score));

// let valueInNumber = Number(score);
// console.log(  valueInNumber);

// let boolean = 0;

// let valueOfBoolean = Boolean(boolean);

// console.log( valueOfBoolean);

// let someNumber = 33;
// let stringNumber = String(someNumber);

// console.log(typeof stringNumber);
// console.log( someNumber);

// *************************Operations******************************

/* The line `let str1 = "hello";` is declaring a variable `str1` and assigning it the value "hello". */
let str1 = "hello";
let str2 = " aditya";
let str3 = str1+str2;
// console.log(str3);

// console.log('1'+ 2);



console.log(1 + '2');
/* The line `console.log(1 + '2');` is concatenating the number 1 with the string '2'. In JavaScript,
when you use the + operator with a string and a number, the number is converted to a string and then
concatenated with the other string. So the result of this line of code would be the string '12'. */

console.log(1 + 2 + '2' ) ;
/* The line `console.log(1 + 2 + '2' ) ;` is performing a series of operations. */

console.log('1'+ 3 + 2);
/* The line `// console.log('1'+ 3 + 2);` is concatenating the string '1' with the numbers 3 and 2. The
result will be the string '132'. */

// console.log(+true);
// console.log(+ "");

// increment operator:-

// let gameCounter = 100;


gameCounter++; // post increment
/* The line `gameCounter++;` is using the post-increment operator to increment the value of the
variable `gameCounter` by 1. This means that the value of `gameCounter` will be incremented after it
is used in the current expression or statement. */
// gameCounter++; // post increment
// console.log(gameCounter);


// let gameCounter = 100;

++gameCounter; // pre increment
/* The line `++gameCounter; // pre increment` is using the pre-increment operator to increment the
value of the variable `gameCounter` by 1. This means that the value of `gameCounter` will be
incremented before it is used in the current expression or statement. */




// console.log(gameCounter);

// *****************************comparison operator***********************

// console.log(2>1);
// console.log(2>=1);
// console.log(2<1);
// console.log(2==1);
// console.log(2!=1);

// console.log(null>0);
// console.log(null == 0);
// console.log(null >= 0);


// console.log(undefined >= null);

console.log(undefined == null); 
/* The line `console.log(undefined == null);` is comparing the values of `undefined` and `null` using
the equality operator (`==`). In JavaScript, the equality operator checks if the values on both
sides are equal, and in this case, it will return `true` because both `undefined` and `null` are
considered equal when using the equality operator. */



// console.log(undefined === null);
// console.log(undefined > null);
// console.log(undefined < null);
// console.log(undefined <= null);

// console.log(undefined >= 0);
// console.log(undefined == 0);
// console.log(undefined === 0);
// console.log(undefined > 0);
// console.log(undefined < 0);
// console.log(undefined <= 0);

















